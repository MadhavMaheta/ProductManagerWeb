import { PriceTrackerDirective } from './price-tracker.directive';

describe('PriceTrackerDirective', () => {
  it('should create an instance', () => {
    const directive = new PriceTrackerDirective();
    expect(directive).toBeTruthy();
  });
});
